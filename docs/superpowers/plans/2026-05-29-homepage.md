# 首页功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增系统首页作为默认入口，左侧展示主流程步骤导航，右侧展示功能入口卡片网格。

**Architecture:** 纯 Streamlit UI 页面，无业务逻辑，通过修改 `st.session_state.selected_menu` 实现页面跳转。

**Tech Stack:** Python, Streamlit, 项目已有的 `ui_theme.py` 设计令牌

---

### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `app.py` | 修改 | 菜单默认项、菜单列表、页面路由 |
| `app.py` | 新增代码 | `show_home_page()`、`_render_workflow_steps()`、`_render_feature_cards()` 三个函数 |

---

### Task 1: 修改菜单系统和默认入口

**Files:**
- Modify: `app.py:185`
- Modify: `app.py:5568-5575`
- Modify: `app.py:5627-5638`

- [ ] **Step 1: 修改默认选中菜单为"首页"**

  将 `app.py:185` 的默认值从 `"数据库配置"` 改为 `"首页"`：

  ```python
  # 改前
  if 'selected_menu' not in st.session_state:
      st.session_state.selected_menu = "数据库配置"
  ```

  ```python
  # 改后
  if 'selected_menu' not in st.session_state:
      st.session_state.selected_menu = "首页"
  ```

- [ ] **Step 2: 在菜单列表首项新增"首页"**

  将 `app.py:5568-5575` 的 `menu_options` 列表首项新增 `("首页", "首页")`：

  ```python
  # 改前
  menu_options = [
      ("数据库配置", "数据库配置"),
      ("地址结构化解析", "地址结构化解析"),
      ("向量预处理", "向量预处理"),
      ("地址匹配", "地址匹配"),
      ("结果管理", "结果管理"),
      ("系统日志", "系统日志"),
  ]
  ```

  ```python
  # 改后
  menu_options = [
      ("首页", "首页"),
      ("数据库配置", "数据库配置"),
      ("地址结构化解析", "地址结构化解析"),
      ("向量预处理", "向量预处理"),
      ("地址匹配", "地址匹配"),
      ("结果管理", "结果管理"),
      ("系统日志", "系统日志"),
  ]
  ```

- [ ] **Step 3: 在页面路由中新增首页分支**

  在 `app.py:5627` 的路由链最前面新增首页分支：

  ```python
  # 改前
  if selected_menu == "数据库配置":
      show_db_config()
  elif selected_menu == "地址结构化解析":
  ```

  ```python
  # 改后
  if selected_menu == "首页":
      show_home_page()
  elif selected_menu == "数据库配置":
      show_db_config()
  elif selected_menu == "地址结构化解析":
  ```

- [ ] **Step 4: 启动应用验证菜单项存在**

  Run: `streamlit run app.py`
  Expected: 侧边栏菜单最上方出现"首页"按钮，且默认高亮选中

---

### Task 2: 实现首页页面函数

**Files:**
- Modify: `app.py`（在路由区域之前插入三个新函数）

- [ ] **Step 1: 插入 `show_home_page()` 主函数**

  在 `app.py` 中路由区域之前（约第 5620 行，`st.divider()` 和 `selected_menu = ...` 之间）插入：

  ```python
  def show_home_page():
      """
      首页：功能导航入口

      布局：左侧 1/3 主流程步骤导航 + 右侧 2/3 功能入口卡片网格
      """
      st.subheader("首页")
      st.caption("地址语义匹配系统 — 选择下方功能开始工作")

      left_col, right_col = st.columns([1, 2])

      with left_col:
          st.markdown("### 📋 主流程")
          _render_workflow_steps()

      with right_col:
          st.markdown("### 🧩 功能入口")
          _render_feature_cards()
  ```

- [ ] **Step 2: 插入 `_render_workflow_steps()` 辅助函数**

  紧跟在 `show_home_page()` 之后插入：

  ```python
  def _render_workflow_steps():
      """渲染左侧主流程步骤导航"""
      steps = [
          ("🗄️", "数据库配置", "连接 PostgreSQL 数据库，查看表结构", "数据库配置"),
          ("🔢", "向量预处理", "配置字段映射，生成地址向量", "向量预处理"),
          ("🔍", "地址匹配", "粗召回 + MGeo 精排，获取匹配结果", "地址匹配"),
          ("📊", "结果管理", "浏览、筛选、导出、人工纠正", "结果管理"),
      ]

      for icon, name, desc, menu in steps:
          with st.container(border=True):
              c1, c2 = st.columns([0.15, 0.85])
              with c1:
                  st.markdown(
                      f"<div style='font-size:1.5em;text-align:center'>{icon}</div>",
                      unsafe_allow_html=True
                  )
              with c2:
                  if st.button(name, key=f"home_step_{name}", use_container_width=True):
                      st.session_state.selected_menu = menu
                      st.rerun()
                  st.caption(desc)
  ```

- [ ] **Step 3: 插入 `_render_feature_cards()` 辅助函数**

  紧跟在 `_render_workflow_steps()` 之后插入：

  ```python
  def _render_feature_cards():
      """渲染右侧功能入口卡片网格（2行 x 3列）"""
      features = [
          ("🗄️", "数据库配置", "连接 PostgreSQL，查看数据表", "数据库配置"),
          ("🔢", "向量预处理", "地址向量化，建立向量索引", "向量预处理"),
          ("🔍", "地址匹配", "粗召回 + MGeo 精排匹配", "地址匹配"),
          ("📊", "结果管理", "浏览、筛选、导出、人工纠正", "结果管理"),
          ("🏷️", "地址结构化解析", "地址 NER 分词，提取省市区街道", "地址结构化解析"),
          ("📝", "系统日志", "查看运行日志与向量调试", "系统日志"),
      ]

      for row_idx in range(0, len(features), 3):
          cols = st.columns(3)
          for col_idx, col in enumerate(cols):
              idx = row_idx + col_idx
              if idx < len(features):
                  icon, name, desc, menu = features[idx]
                  with col:
                      with st.container(border=True):
                          st.markdown(
                              f"<div style='font-size:2em;text-align:center'>{icon}</div>",
                              unsafe_allow_html=True
                          )
                          if st.button(name, key=f"home_card_{name}", use_container_width=True):
                              st.session_state.selected_menu = menu
                              st.rerun()
                          st.caption(desc)
  ```

- [ ] **Step 4: 语法验证**

  Run: `python -c "import py_compile; py_compile.compile('app.py', doraise=True); print('OK')"`
  Expected: `OK`

---

### Task 3: 验证与提交

- [ ] **Step 1: 启动应用验证首页渲染**

  Run: `streamlit run app.py`
  Expected:
  1. 默认进入"首页"页面
  2. 页面左侧显示 4 个主流程步骤（数据库配置、向量预处理、地址匹配、结果管理）
  3. 页面右侧显示 6 个功能入口卡片（2 行 × 3 列）
  4. 点击左侧步骤或右侧卡片能正确跳转到对应页面
  5. 侧边栏"首页"按钮默认高亮

- [ ] **Step 2: 验证页面跳转**

  依次点击：
  - 左侧"数据库配置" → 跳转到数据库配置页面
  - 右侧"地址匹配" → 跳转到地址匹配页面
  - 右侧"结果管理" → 跳转到结果管理页面
  - 侧边栏"首页" → 返回首页

- [ ] **Step 3: Commit**

  ```bash
  git add app.py
  git commit -m "feat: 新增系统首页功能

  - 侧边栏菜单新增'首页'入口，设为默认打开页面
  - 首页左侧展示主流程步骤导航（数据库配置→向量预处理→地址匹配→结果管理）
  - 首页右侧展示6个功能入口卡片网格（2行x3列）
  - 点击步骤或卡片可跳转对应功能页面"
  ```

---

### Self-Review Checklist

- [x] **Spec coverage**: 左侧流程步骤 ✓、右侧功能卡片 ✓、菜单调整 ✓、路由 ✓
- [x] **Placeholder scan**: 无 TBD、TODO、未定义引用
- [x] **Type consistency**: `selected_menu` 字符串值与现有菜单键名一致
